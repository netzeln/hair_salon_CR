<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css">
        <link rel="stylesheet" href="/css/bootstrap-theme.min.css" type="text/css">
        <link rel="stylesheet" href="/css/styles.css" type="text/css">
        <meta charset="utf-8">
        <title>Revolutionary Styles!</title>
    </head>
    <body>
        <div class="container">
            <h1>Revolutionary Styles</h1>
            <div class ="container">
                <h3> Update the Revolutionary Crew </h3>
                <form action="/add_stylist" method="post">
                    <label for="stylist_name">Add Stylist Name:</label>
                    <input type="text" name="stylist_name" value="" required>
                    <button type="submit" class="btn btn-sm btn-primary">Add</button>
                </form>
            </div>
            <div class="container">
                {% if stylists is not empty %}
                    <h3>Current Stylists</h3>
                    <ul>
                        {% for stylist in stylists %}
                        <li><a href="/stylist/{{stylist.getId}}">{{stylist.getStylistName}} </a></li>
                        {% endfor %}
                    </ul>
                    {% else %}
                    <h2> Note to Self: Hire Stylists!</h2>
                {% endif %}

            </div>
            <div>
                <h3>View Current Clients</h3>
                <a href="/view_clients"><button class="btn btn-sm btn-info">View</button></a>
            </div>
            <div class="container">
                <h3>Fire the Staff</h3>
                <form class="delete" action="/delete_all" method="post">
                    <button type="submit" class="btn btn-sm btn-danger">FIRE!</button>
               </form>
            </div>
        </div>
    </body>
</html>
